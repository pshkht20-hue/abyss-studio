# GLB Assets

## Required (hero)

- `shark.glb` — Quaternius rigged shark (`Swim`, `Swim_Bite` animations)

## Optional (future sections)

- `manta.glb`, `whale.glb`, `dolphin.glb`, `ship.glb`, `coral.glb`, `kelp*.glb`, etc.